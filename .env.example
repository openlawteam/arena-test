DATABASE_URL="postgresql://USER:PASSWORD@HOST/DB?sslmode=require"
ARENA_COOKIE_SECRET="replace-with-a-long-random-secret"
# Optional once the official Grok webhook hostname is confirmed:
# GROK_WEBHOOK_HOSTS="example.grok-webhook-host.com"
